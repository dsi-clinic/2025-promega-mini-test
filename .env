# Variables that control deterministic results
CUBLAS_WORKSPACE_CONFIG=':4096:8'    # control workspace behavior
TF_DETERMINISTIC_OPS='1'    # TensorFlow deterministic CUDA operations (if using GPU)
TF_CUDNN_DETERMINISTIC='1'    # Deprecated legacy variable that covers version < TF 2.8